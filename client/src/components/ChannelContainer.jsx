import React from 'react';

const ChannelContainer = () => {
    return (
        <div>
            React Chatting App, built with React and Stream.io
        </div>
    );
}

export default ChannelContainer;